Instructions:
1. for every functionality that is created, ensure documentation is created and stored in the docs subfolder in markdown format
2. activate virtual environment before executing any python script.
3. always commit changes to local git repository.